<%= turbo_stream.replace "__#{dom_id(@image)}" do %>
  <%= render partial: 'line', locals: {image: @image } %>
<% end %>

<%= turbo_stream.replace "flash_notices" do %>
  <%= render partial: "layouts/flash_notices" %>
  <% if @image.errors.any? %>
  <%= render partial: "errors", locals: {resource: @image} %>
  <% end %>
<% end %>
